import React from 'react';

const Loading = () => {
    return (
      <div className='h-screen flex justify-center items-center'>
        <button className="btn btn-primary text-white loading">loading</button>;
      </div>
    );
};

export default Loading;